spring:
  flyway:
    baseline-on-migrate: true
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/test_migration?useSSL=false
server:
  port: 8081
